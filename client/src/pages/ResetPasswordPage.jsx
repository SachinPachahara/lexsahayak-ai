import { useState } from 'react';import { useSearchParams,useNavigate } from 'react-router-dom';import toast from 'react-hot-toast';import { api } from '../lib/api';import { AuthShell } from './LoginPage';
export default function ResetPasswordPage(){const [params]=useSearchParams(),nav=useNavigate();const [password,setPassword]=useState('');async function submit(e){e.preventDefault();try{await api('/auth/reset-password',{method:'POST',body:{token:params.get('token')||'',password},auth:false});toast.success('Password changed');nav('/login');}catch(e){toast.error(e.message)}}return <AuthShell title="Choose a new password" subtitle="Reset links expire after 30 minutes."><form onSubmit={submit} className="space-y-4"><div><label className="label">New password</label><input className="input" type="password" minLength={10} required value={password} onChange={e=>setPassword(e.target.value)}/></div><button className="btn btn-primary w-full">Reset password</button></form></AuthShell>}
