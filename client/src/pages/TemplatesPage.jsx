import { useEffect,useMemo,useState } from 'react';
import { Link } from 'react-router-dom';
import { Search,ArrowRight,FileText,Sparkles } from 'lucide-react';
import { api } from '../lib/api';
const groups=['All','Personal','Business','Startup','Consumer','Career','Custom'];
export default function TemplatesPage(){const [templates,setTemplates]=useState([]),[q,setQ]=useState(''),[group,setGroup]=useState('All');useEffect(()=>{api('/documents/templates').then(d=>setTemplates(d.templates));},[]);const items=useMemo(()=>templates.filter(t=>(group==='All'||t.category===group)&&(`${t.name} ${t.description}`.toLowerCase().includes(q.toLowerCase()))),[templates,q,group]);return <div className="space-y-6"><div><div className="text-sm font-black text-indigo-500">GUIDED DRAFTING</div><h1 className="mt-1 text-3xl font-black tracking-tight">Choose a legal document workflow</h1><p className="mt-2 text-sm text-slate-500">Each template validates structured facts before AI generation, reducing prompt ambiguity and making outputs easier to audit.</p></div><div className="panel p-4"><div className="flex flex-col gap-3 lg:flex-row lg:items-center"><div className="relative flex-1"><Search size={18} className="pointer-events-none absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400"/><input className="input has-search-icon" placeholder="Search templates…" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="flex flex-wrap gap-2">{groups.map(g=><button key={g} onClick={()=>setGroup(g)} className={`btn py-2 ${group===g?'btn-primary':'btn-secondary'}`}>{g}</button>)}</div></div></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(t=><Link to={`/app/generate/${t.id}`} className="panel group p-5 transition hover:-translate-y-1 hover:shadow-xl" key={t.id}><div className="flex items-start justify-between"><div className="stat-icon"><FileText size={20}/></div><span className="badge">{t.category}</span></div><h3 className="mt-5 text-lg font-black">{t.name}</h3><p className="mt-2 min-h-12 text-sm leading-6 text-slate-500">{t.description}</p><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4 text-xs dark:border-slate-800"><span className="flex items-center gap-1 font-bold text-indigo-500"><Sparkles size={13}/> {t.fields.length} guided fields</span><span className="flex items-center gap-1 font-black text-slate-700 group-hover:text-indigo-500 dark:text-slate-300">Start <ArrowRight size={14}/></span></div></Link>)}</div></div>}
