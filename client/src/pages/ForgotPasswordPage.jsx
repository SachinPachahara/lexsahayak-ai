import { useState } from 'react';import toast from 'react-hot-toast';import { Link } from 'react-router-dom';import { api } from '../lib/api';import { AuthShell } from './LoginPage';
export default function ForgotPasswordPage(){const [email,setEmail]=useState(''),[link,setLink]=useState('');async function submit(e){e.preventDefault();try{const d=await api('/auth/forgot-password',{method:'POST',body:{email},auth:false});setLink(d.devResetLink||'');toast.success('If the account exists, reset instructions were sent.');}catch(e){toast.error(e.message)}}return <AuthShell title="Reset password" subtitle="Request a short-lived password reset link."><form onSubmit={submit} className="space-y-4"><div><label className="label">Email</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><button className="btn btn-primary w-full">Send reset instructions</button>{link&&<a className="block break-all rounded-xl bg-amber-50 p-3 text-xs font-semibold text-amber-800" href={link}>Development reset link: {link}</a>}<Link to="/login" className="block text-center text-sm font-bold text-indigo-500">Back to sign in</Link></form></AuthShell>}
