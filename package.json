{
  "name": "lexsahayak-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Production-style MERN + LangChain legal document intelligence platform",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n api,web -c auto \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "test": "npm run test --prefix server && npm run test --prefix client",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
